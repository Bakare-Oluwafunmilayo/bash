# -*- mode: ruby -*-
# vi: set ft=ruby :

Vagrant.configure("2") do |config|

    config.vm.box = "ubuntu/trusty64"

    config.vm.hostname = "dev-linux"

    #config.vm.network "private_network",, type: "dhcp" 10.0.2.15/24 127.0.0.1/8
    
    config.vm.provider "virtualbox" do |vb|
      vb.memory = "2048"
      vb.cpus = 2
    end

  end


  #create two groups: developer and intern
  # add a user with name oyeola to developer group
  # add two users to intern groupwq
   